# .gitignore for mental-health-assistant/

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
pip-log.log
pip-log.txt
.env # IMPORTANT: Contains sensitive API keys, DB credentials etc.
/backend/chroma_db/ # ChromaDB's generated vector database files

# Node.js / Next.js
node_modules/
.next/
.turbo/
out/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env.local # Local environment variables for frontend (contains API keys/secrets)
.env.development.local
.env.test.local
.env.production.local
*.log # Log files generated by Node/Next (e.g., next-dev.log)

# Docker volumes
# These are managed by Docker Compose and should not be in Git
# The actual paths might vary, but typically it's named volumes
# and not raw folders in your project unless you changed paths.
# If you put chroma_data directly in your project root, add it here.
# Examples if they were in your project dir:
# postgres_data/
# ollama_models/
# chroma_data/
# You generally want to keep Docker Compose volumes outside the project.

# Operating System / IDE specific files
.DS_Store # macOS
.vscode/ # VS Code settings specific to your machine (optional, depends on team preference)
.idea/ # IntelliJ / PyCharm
*.swp # Vim swap files
*.sublime-project
*.sublime-workspace
.project
.settings/

# Other Generated Files
*.tmp
*.temp
*.bak
*.orig